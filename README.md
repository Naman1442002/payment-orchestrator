# payment-orchestrator
the system’s main job is to coordinate/orchestrate payment flow between users, services, banks, gateways, retries, notifications, and transaction states — just like PayU/Razorpay.
